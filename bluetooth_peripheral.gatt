// reference
// https://github.com/bluekitchen/btstack/blob/master/example/gatt_streamer_server.gatt


// name
PRIMARY_SERVICE, GAP_SERVICE
CHARACTERISTIC, GAP_DEVICE_NAME, READ, "RC Car"

PRIMARY_SERVICE, GATT_SERVICE
CHARACTERISTIC, GATT_DATABASE_HASH, READ,


PRIMARY_SERVICE, AAAA

// phone -> car
// steering + throttle
CHARACTERISTIC, BBBB, WRITE_WITHOUT_RESPONSE | DYNAMIC

// car -> phone
// responses back
CHARACTERISTIC, CCCC, NOTIFY | DYNAMIC
